import { ShowHide } from './show-hide';

describe('ShowHide', () => {
  it('should create an instance', () => {
    const directive = new ShowHide();
    expect(directive).toBeTruthy();
  });
});
